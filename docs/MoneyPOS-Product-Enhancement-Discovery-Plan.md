# MoneyPOS 产品优化需求台账与初步实施方案

> 初始记录日期：2026-09-22；当前事实已核对至 2026-09-23 `dev`。本文登记需求、源码现状、风险和后续建议；“完成”项保留实施事实，不构成新的改动授权。

## 目标

在不破坏现有收银、退款、库存和会员资产事务的前提下，优先提升门店操作安全性、收银台可用性和管理端信息密度；随后再评估会员外部查询与会员产品能力。

## 需求台账

| 编号 | 需求 | 当前源码事实 | 建议目标 | 优先级 |
| --- | --- | --- | --- | --- |
| UX-1.1 | 后台独立密码认证 | 已实施：POS 与后台为独立 Electron 会话，后台从账号下拉和密码登录开始；关闭后清理后台会话，重开必须重新认证。 | 已完成验收；默认密码与通用 JWT 撤销另列安全任务，不混入本切片。 | 完成 |
| UX-1.2 | POS 用户名下拉登录 | 已实施：普通 POS 与后台共用最小账号候选接口；POS 可预选仍启用的上次账号，但始终要求输入密码。 | 不改变认证、路由和收银员切换语义。 | 完成 |
| UX-2.1 | 关闭桌面端前确认 | 已实施：主 POS 窗口关闭会显示原生“继续收银 / 确认退出”；取消不改变窗口或 Java 后端，确认后才统一退出。未结算购物车不自动保存，已挂单订单保留。 | Windows 打包版按实施记录验证标题栏、Alt+F4、任务栏和连续关闭路径。 | 完成代码；实机回归按需 |
| UX-3.1 | 零库存商品提示与补货 | 已实施：零库存或超库存商品仍可进购物车，但异常行常驻提示、浅红高亮并提供补货；补货成功后刷新购物车库存并重新试算。试算/正式结算的服务端库存校验及 GMS 原子扣减仍是最终防线。 | 当前没有前端收款按钮硬拦截、聚合库存面板或库存错误静默；如需这些体验，再独立设计。 | 完成 |
| UX-4 | 库存提示不遮挡菜单且可关闭 | 已部分实现：库存异常在购物车行内和结算窗内处理，避免正式结算库存错误直接落到全局顶部提示；补货是明确动作。 | 未实现全局可关闭、可聚合的库存提示中心；仅在实际使用证明需要时另立任务。 | 部分完成 |
| UX-5 | 会员仓库 / 品牌会员权益 | 已完成业务建模并开始 `ME-1`：QUANTITY 为已售延迟提货，AMOUNT 为品牌权益包及补差提货，TARGET 为即时商品销售达标升级；第一切片已落 UMS 档位与账本基础。 | 按 `MoneyPOS-ME-1-Member-Entitlement-Implementation-Checklist.md` 逐切片实施；不向普通 POS 增加权益操作。 | 进行中 |
| UX-6 | 前台切换收银员 | 已实施安全切换：必须先清空当前购物车，再从本机候选账号选择下一位收银员并输入密码；成功后刷新当前认证身份。订单/交接班仍按后端认证上下文的 `create_by` 归属。 | 不是锁屏、挂单转交或非空购物车处置流程；这些需求如有必要须另行设计。 | 完成 |
| UX-7 | 现金结账自动开钱箱 | 已实施：结账小票仅在订单支付明细存在**正额现金支付**且打印配置允许自动开箱时，写入开箱 ESC/POS 指令；扫码、余额等非现金结账不自动开箱，补打也不自动开箱。保留受 `pos:cashier` 权限保护的手动开箱入口。 | 当前规则不是“仅有找零时开箱”；找零金额仅展示在小票和结算结果中，不参与自动开箱判断。 | 完成代码；钱箱实机确认待门店启用 |
| UX-8 | 会员同步至 Cloudflare Worker，并建查询网站 | 当前会员查询属于本地 Spring/数据库，不能直接暴露给互联网。 | 采用本地应用主动同步最小必要会员资料到 Worker 后的受控存储；网站只经 Worker 查询，具有鉴权、限流、审计和隐私控制。 | P2（设计优先） |
| UX-9 | 汇总和重分类后台分析 | HOME、FIN、OMS、GMS 分散提供图表、排行、瀑布、风险、库存和流水分析。 | 以门店决策任务重新分组，先做导航/信息架构与指标盘点，避免复制或改变既有财务公式。 | P1（发现） |

## 已确认的设计边界

1. **库存拦截不能只做前端。** 前端提示可改善体验，但库存扣减必须继续由 GMS 原子命令在 TRADE 的结算事务内兜底，避免并发超卖。
2. **收银员切换是身份切换。** 它必须重用账号认证和权限体系，订单操作人必须由后端认证上下文决定，不能把收银员姓名作为前端可编辑字段提交。
3. **钱箱是硬件动作。** 当前自动开箱以已持久化订单支付明细中的正额现金支付为条件，并受打印配置控制；不得用界面试算值或“是否找零”推断。结账打印、补打、交班打印和人工开箱必须保持不同语义。
4. **关闭确认需要在 Electron 主进程处理。** 仅在 Vue 页面拦截不能覆盖窗口标题栏、任务栏和系统关机路径；确认后再终止后端，取消则保持窗口和收银状态。
5. **Cloudflare 不得直连门店数据库。** 同步应是门店端向外推送，Worker 只接收经签名/认证的最小数据；公开网站不得暴露余额、券、交易明细、手机号全文或可猜测的会员标识。
6. **报表重组先不改指标。** 当前 FIN/HOME 的读取边界和交易公式已完成架构收敛，信息架构改造必须先证明“同一数据、不同呈现”，再讨论新指标或计算口径。

## 推荐实施顺序

### 已完成：安全与防误操作

1. UX-1.1 已隔离 POS 与后台 Electron 会话，后台窗口每次打开均需选择账号并输入密码。
2. UX-1.2 已将普通 POS 登录统一为本机候选账号下拉加密码；候选接口仅面向回环地址、默认租户且返回最小字段。
3. UX-2.1 已在 Electron 主进程实现退出确认；非空购物车的产品语义为确认后丢弃、已挂单保留。

仍可在 Windows 安装包复核：后台不能复用 POS 身份、取消关闭不丢失界面状态、确认关闭后 Java 子进程正常结束。

### 已完成：收银台即时反馈（最小切片）

1. 已实现零库存/超库存购物车行的常驻提示、视觉强调和补货入口；补货后刷新库存并重新试算。
2. 试算和正式结算库存失败由 POS 内部处理，正式结算仍依赖现有原子校验，失败时保留购物车。
3. 未实现聚合问题面板或前端硬禁用；这不是遗漏的隐式待办，须由实际使用反馈单独授权。

验收事实：收银员无需等待提示超时即可从异常行进入补货；任何路径都不能绕过库存扣减守卫。

### 已完成：人员与硬件语义（当前范围）

1. UX-6 已实现“空购物车 + 下一位账号密码”的安全收银员切换；非空购物车的继续、挂单或清空策略未在本轮扩展。
2. UX-7 已将结账自动开箱收敛为“正额现金支付 + 自动开箱配置”；补打不自动开箱，手动开箱保留独立受权入口。
3. 钱箱实机验收仍应覆盖现金有/无找零、扫码、余额、混合支付、补打和手动开箱；这不改变当前已实现的判断规则。

### 阶段 D：产品发现与管理端整理

1. UX-5 召开/记录会员产品仓库发现：目标用户、商品类型、价格/库存/有效期、是否可在 POS 销售、是否关联会员等级/券/余额、退款规则及报表口径。
2. UX-9 盘点现有页面与指标，按“今日经营、收银交班、商品库存、会员经营、风险与审计”形成候选导航；标记保留、合并、下沉和废弃候选，但不删除任何能力。
3. 对每个被批准的业务领域分别设计路由、权限、数据所有者、回归和迁移计划。

### 阶段 E：Cloudflare 会员查询（独立项目）

1. 明确网站使用者（店员、会员本人或总部）、查询键（手机号后四位/会员号/扫码）和允许展示字段。
2. 设计数据生命周期：本地变更 outbox、重试/幂等、删除或脱敏同步、断网补偿、可观测审计。
3. 设计 Worker API 与认证：门店上传凭证、网站身份认证、RBAC、速率限制、机器人防护、查询审计和密钥轮换。
4. 选择 Cloudflare 存储与部署边界后，再建立独立网站仓库或经授权在本仓库新增站点；先用脱敏测试数据完成端到端验证。

## 在实施前需要确认的产品决策

1. 默认账号、强制改密与 JWT 通用撤销仍是独立安全议题，不属于已完成的后台会话隔离。
2. 当前确认退出会丢弃未结算购物车、保留已挂单；若要强制挂单或清空前确认，须另立产品决策。
3. 当前零库存商品允许加入购物车并通过行内补货处理，最终由服务端结算和原子扣减拦截。
4. 当前钱箱在存在正额现金支付时自动打开，不以找零为条件；若要改成“仅找零”或增加其他支付规则，须另立硬件语义任务并实机验证。
5. “会员产品仓库”具体想管理什么类型的产品，以及是否涉及库存、核销、支付和退款？
6. Cloudflare 网站的访问对象和会员数据授权方式是什么？这决定是否需要会员本人登录、短信验证或仅供内部员工使用。

## 实施纪律

每一项实施均需单独编号与设计，明确路由、权限、数据库/Flyway、事务、隐私影响和专项回归；不得把上述多项合并为一次大改。涉及 Cloudflare 的网络、账号、密钥、生产部署或个人信息外发，必须在单独授权后进行。
